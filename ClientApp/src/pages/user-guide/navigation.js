import React from 'react';

export default  props => {
    return (
        <>
            <h2>Navigation</h2>

            <p>
                On startup you see the main screen (time tile). It show the time and the current weather (if correct configure).<br/>
                Now you can swipe with you fingers up, down, left and right between the four main screens.<br />
                The four screens are organized in time, apps, note and setup tile
           </p>
        </>
    );
}